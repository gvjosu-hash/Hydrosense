[build]
  # "npm run build" ya incluye "prisma migrate deploy" (ver package.json),
  # así que las migraciones se aplican solas en cada deploy sin importar la
  # plataforma de hosting.
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"
